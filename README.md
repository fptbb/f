# f
most "minimal" fastfetch alternative (now in assembly)

Install f: `curl -sL --fail "https://github.com/fptbb/f/releases/download/f/f" -o "$HOME/.local/bin/f" && chmod +x "$HOME/.local/bin/f"`

Run f: `f`

<sup><sub>(yes, this is a joke)</sub></sup>